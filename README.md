# 收入记录本

一个适配手机端的家教收入记录网页应用。

## 使用方式

直接打开 `index.html` 即可使用。

## 部署到 GitHub Pages

将本文件夹中的 `index.html`、`style.css`、`app.js` 上传到仓库根目录，然后开启 GitHub Pages。

## 数据说明

收入记录保存在当前浏览器的 localStorage 中。别人访问同一个网页时，不会看到你的本地数据。长期使用建议定期在应用内导出备份。
